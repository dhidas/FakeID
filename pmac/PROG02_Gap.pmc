#include "QVariables.pmc"

&2
#1->I
#2->I
#3->I
#4->I

Open PROG 2 Clear

LINEAR
ABS
TA100
TS0
TM1000

FRAX(A,B,C,U)
F(500)

A(Q_A_KinDemandPos) B(Q_B_KinDemandPos) C(Q_C_KinDemandPos) U(Q_U_KinDemandPos)
Dwell0

Close
